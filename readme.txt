to run tests and generate allure reports run the following command
Windows
>gradlew.bat clean test
Linux
>gradlew clean test
the web page for allure report is located here:
<project root dir>/build/reports/tests/test/index.html